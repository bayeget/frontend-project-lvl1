name: lint-check

on:
  push:
    branches:
      - '**'
    tags:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: npm install
        # Install project
        run:
          make install
      - name: Run linter
        # Run Linter
        run:
          make lint